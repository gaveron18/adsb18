# Session 2026-03-30b — Bug fixes: messageRate, archive GROUP BY, planespotters CORS, route API

## Что сделали

### Issue 1 — Messages/sec (ИСПРАВЛЕН, коммит 97ee3f2)
- Pi возвращает messages как накопленный счётчик. Добавили вычисление messageRate на сервере в main.py.
- Результат: 16.0/sec вместо 117877/sec

### Issue 2 — Archive GROUP BY обрезал треки (ИСПРАВЛЕН, коммит fc4fd1b)
- GROUP BY icao, callsign + WHERE callsign IS NOT NULL теряло первые точки рейса без позывного.
- Фикс: GROUP BY icao, MAX(callsign), убрали фильтр.
- NWS743: +74 точки, first_seen на 1.5 мин раньше.

### Issue 3 — planespottersAPI CORS (ИСПРАВЛЕН, коммит f220859)
- planespottersAPI = false в config.js.
- nginx: location = /config.js с Cache-Control no-store.
- index.html: config.js?v=2 для сброса кеша.
- 0 ошибок в консоли.

### Issue 4 — Route колонка пустая (ИСПРАВЛЕН, коммит ede1bab)
- routeApiUrl =  — отключили внешний сервис api.adsb.lol/api/0/routeset.

## Nginx — важно
- Реальный конфиг: /etc/nginx/sites-enabled/adsb18 (НЕ sites-available/adsb18)
- nginx.conf в репо синхронизирован с реальным (коммит 01e3b71)
- Сервис через systemd, не Docker

## GitHub
- Все коммиты запушены: ede1bab -> 40a8b58 -> 01e3b71 -> f220859 -> fc4fd1b
