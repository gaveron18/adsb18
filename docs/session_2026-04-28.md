# Сессия 2026-04-28

## Диагностика: ловит дальние борта, не ловит ближние

### Жалоба
Около Pi3 пролетало ~50 рейсов за день, в БД ближних рейсов почти нет — только дальние.
Гипотеза Андрея: слишком большое усиление (gain).

### Что проверено

**Конфиг readsb на Pi** (`/etc/default/readsb`):
```
RECEIVER_OPTIONS="--device 0 --device-type rtlsdr --gain auto --ppm 0"
```
- `--gain auto` — readsb сам подбирает gain
- autogain-скрипт `/usr/local/bin/readsb-gain` установлен, **но не активен**
  (нет systemd timer'а, кэш `/var/cache/readsb-autogain/` пуст)

**Текущее значение gain в эфире** (`stats.json`):
- gain_db плавает между 22.9 и 0.0 (нерегулярная запись поля при `--gain auto`)
- 22.9 — стандартное значение из таблицы RTL-SDR, середина шкалы

### Snapshot статистики `stats.json` — для сравнения после смены gain

Снято 2026-04-28 ~21:43 MSK через туннель `:30092`.

| Период | peak_signal | signal | noise | strong_signals | bad | accepted |
|---|---|---|---|---|---|---|
| last1min (тихо)         | −27.9 dBFS | −34.8 | −44.8 | 0      | 3 438         | 791       |
| last5min                | −27.9      | −33.0 | −44.8 | 0      | 16 746        | 2 612     |
| **last15min (близкий борт)** | **−1.8**   | −8.9  | −32.6 | **1333** | **18 732 689** | 20 630 |
| total (за всё время)    | −0.6       | −9.4  | −30.6 | 174 364 | 2 745 621 459 | 2 749 717 |

`samples_dropped: 0` во всех окнах → USB/Pi не перегружены, проблема в РЧ-тракте.

### Интерпретация

1. **Когда близкий борт пролетает — приёмник на грани насыщения АЦП**
   (peak −1.8 dBFS из 0). Это не теория, фиксируется в каждом 15-мин окне.
2. **Шумовой пол подскакивает на 12 dB** при близком борте (−44.8 → −32.6) —
   характерный признак работы AGC RTL-SDR на пределе.
3. Соотношение `bad / accepted` ≈ **1000:1** — большая часть громких пакетов
   декодируется как мусор и отбрасывается. Это и есть пропущенные ближние борта.
4. `strong_signals = 1333` за 15 мин при пролёте близкого vs `0` в тихие минуты —
   локальные борта **систематически** заходят в зону перегруза.

### Вердикт
Гипотеза Андрея подтверждается: **gain слишком высокий** для близких бортов.
Дальние принимаются нормально, потому что приходят слабым сигналом и не вызывают перегруз.

### Эксперимент в живом эфире (вечер того же дня)

Pi оказался доступен — провели эксперимент со снижением gain через
`sudo readsb-gain <value>` (hot-reload через `/run/readsb/setGain`, без рестарта).

**Hot-reload работает** (это важно): `MainPID` readsb не менялся,
gain применяется к железу без даунтайма. В логе виден `rtlsdr: tuner gain set to N dB`.

**Замечание про `--gain auto`**: при ошибочном вызове `readsb-gain --help`
скрипт интерпретирует `--help` как новое значение gain и записывает
в конфиг `--gain --help`. readsb принимает это как 0.0 dB и работает «глухим»
до следующей правки. **Не вызывать `readsb-gain --help`!** Скрипт без валидации.

#### Поймали два ключевых события

**Событие 1 — STW133 на посадке** (Smart Avia, посадка в Б.Савино)
- type: `mode_s` (но `lastPosition` существовала когда борт был далеко →
  ADS-B Out у самолёта **есть**, мы потеряли его при overload)
- Прошёл от 10 800 ft до 700 ft за 5 минут
- Пиковый rssi: **−1.6 dBm** (на 5 675 ft)
- Туннель `:30092` дважды отдавал `ERR fetch failed` в момент пика

**Событие 2 — PBD6512 на взлёте** (Победа)
- type: `mode_s`, `lastPosition: none` → у борта **нет ADS-B Out** вообще
- Пиковый rssi: **−1.6 dBm** (на 8 500 ft, разворот после взлёта)
- ADS-B позиции никогда не было — это особенность самолёта, не нашего приёмника

**Событие 3 — два крейсерских борта прямо над станцией** (для теста gain=12.5)
- SVR624 (Уральские, FL370) rssi=−2.5..−7
- CES592 (China Eastern, FL350) rssi=−2.8..−6.1
- Одновременно в эфире, оба громкие

#### Сравнительная таблица по всем gain'ам

| gain | пик. rssi борта | **peak_signal** | strong/мин | noise | bad/15min | accepted/15min |
|---|---|---|---|---|---|---|
| auto/22.9 (baseline) | −24..−2 | **−0.7..−1.8** | 89..1333 | −30..−32 | 18.7M | 20 630 |
| 19.7 (STW)         | −2.0    | **−0.9**       | 332      | −31.6  | 31.3M | 32 887 |
| 16.6 (PBD)         | −1.6    | **−1.0**       | 107..298 | −31.9  | 21.1M | 33 000 |
| **12.5** (SVR/CES) | **−2.5** | **−1.2**      | 351..428 | **−35.2** | 0.5M | ~32 000 |

#### Главное физическое открытие

**Снижение gain с 22.9 до 12.5 (на 10.4 dB) изменило peak_signal на 0.6 dB.**

Это означает:
- Clipping АЦП происходит **до** регулируемого VGA внутри R820T2
- `--gain` физически НЕ может убрать насыщение от очень близких бортов (rssi < −5 dBm)
- ADS-B позиции таких бортов **будут теряться при любом значении gain** на этом железе
- Доказательство получено в живом эфире, не теоретически

#### Что улучшилось при gain=12.5 (несмотря на тот же peak)

- **noise упал на 4 dB** (−31 → −35) — лучше декодируются дальние борта
- **bad** упало в 60 раз (31M → 0.5M за 15 мин) — пакеты чище, CPU спокойнее
- Дальние ловятся как раньше (rssi −24..−27 продолжают приниматься)
- Строки `strong/мин` остались высокими, но это от наличия близких — peak их формирует физически

#### Принятое решение

**Оставить gain=12.5** (применено через `sudo readsb-gain 12.5`, persistent в `/etc/default/readsb`).

Аргументация:
- лучший шумовой пол для дальних
- меньше bad-пакетов
- overload на близких всё равно неизбежен на этом железе — лечится только аппаратно
- если получится аттенюатор — gain вернём в `auto`, тогда readsb-autogain заработает поверх pad'а

#### Что нужно купить

**RF-аттенюатор 6 dB или 10 dB**, разъёмы SMA M-F, частотный диапазон ≥ 1.5 ГГц.
Кандидаты:
- Mini-Circuits VAT-6+ или VAT-10+ (премиум, ~1500₽)
- VEGATEL AT-6 / AT-10 (российский бренд, есть на Озон)
- Generic SMA pad с AliExpress (~300₽)

При установке: между антенной и RTL-SDR. Не трогать gain до и после — чистый
эксперимент. Если 6 dB pad не уберёт overload — добавить ещё 3 dB stack'ом.

### Третье подтверждение overload — SDM6074 (после реконнекта)

После того как сессия терминала оборвалась (`Connection closed by remote host`),
Claude переподключился — gain=12.5 остался в проде, монитор продолжал писать в
`/tmp/gain_monitor.log` без перерыва.

В 20:38 застал взлёт **SDM6074** (Smartavia, IL-86? RA-... — точно не знаю):
прошёл от 525 ft до 5 100 ft за 2.5 минуты прямо над станцией.

Динамика над станцией (gain=12.5):

| Время | alt ft | rssi dBm | peak dBFS | strong/мин |
|-------|--------|----------|-----------|------------|
| 20:38:50 | 1 375 | −7.6 | −6.0 | 0 |
| 20:39:07 | 2 100 | −2.8 | −1.4 | 21 |
| 20:39:25 | 2 825 | −2.1 | −1.2 | 130 |
| 20:39:54 | 3 775 | −3.0 | −1.1 | 303 |
| **20:40:04** | **3 950** | **−2.8** | **−1.1** | **312** ← пик |
| 20:41:09 | 5 100 | −1.9 | −1.3 | 231 |

**Тип в aircraft.json: mode_s, lat/lon=None** — позиции по борту нет (как у STW и PBD).
Не можем отличить «нет ADS-B Out» от «overload съел DF17» без аттенюатора.

#### Сводная таблица всех 4-х ключевых событий сессии

| Борт | gain | rssi пик | peak | strong/мин | результат |
|------|------|----------|------|------------|-----------|
| STW133 (посадка) | 19.7 | −2.0 | −0.9 | 332 | overload |
| STW133 (посадка) | 16.6 | −1.6 | −1.0 | 298 | overload |
| PBD6512 (взлёт) | 16.6 | −1.6 | −1.0 | 107 | overload |
| **SDM6074 (взлёт)** | **12.5** | **−1.9** | **−1.1** | **312** | overload |

Снижение gain с 22.9 до 12.5 (на 10.4 dB) изменило peak на 0.6 dB. Гипотеза
«clipping АЦП до VGA» подтверждена окончательно — четыре независимых события
дают одинаковый peak ≈ −1 dBFS при rssi ≈ −2 dBm.

### Snapshot файлы для будущих сравнений

- `docs/stats_before_gain_20260428_1904.json` — gain=auto/22.9, тихая минута
- `docs/stats_after_g19.7_20260428_1922.json` — gain=19.7 после окна с STW
- `docs/stats_quiet_g16.6_20260428_1928.json` — gain=16.6 после посадки STW
- `docs/stats_after_g16.6_20260428_2008.json` — gain=16.6 финал, перед сменой на 12.5

### Что осталось в проде (на момент завершения сессии)

- **gain = 12.5** (persistent в `/etc/default/readsb` на Pi)
- readsb работает на старом MainPID — рестарта не было, только hot-reload
- все остальные параметры (PPM, gain auto-script) не трогали
- монитор `/tmp/gain_monitor2.sh` остановлен в конце сессии
- эталон «тихая минута gain=12.5»: peak=−13.4, noise=−39, strong=0

### План на следующую сессию (когда придёт аттенюатор)

Заказан **VEGATEL AT-10 (R00527)** — пассивный аттенюатор 10 dB, SMA M-F,
0–2700 МГц, 50 Ом. На Yandex Market / Озон.

Когда придёт:

1. Снять snapshot `stats.json` ДО установки (для сравнения), сохранить в `docs/`
2. Открутить кабель антенны от RTL-SDR на Pi
3. Прикрутить AT-10 inline: антенный кабель → AT-10 → RTL-SDR
4. Не трогая gain (оставить 12.5), снять snapshot ПОСЛЕ
5. Дождаться громкого борта (взлёт/посадка в Б.Савино), измерить peak_signal —
   ожидание: peak упадёт на 8–10 dB (с −1 до −9..−11 dBFS)
6. **Проверить главное** — появились ли близкие борта с координатами на карте?
   Если STW/SDM/подобные при ближайшем пролёте показывают `type: adsb_icao`
   и заполненные lat/lon — задача решена ✅
7. Вернуть `--gain auto` через `sudo readsb-gain auto` — теперь readsb-autogain
   заработает корректно поверх 10 dB pad'а
8. Снять третий snapshot для финального сравнения
9. Записать в `docs/session_<новая_дата>.md` + закрыть открытую задачу в `CLAUDE.md`

Если 10 dB окажется слишком много (дальние просели заметно по `count(distinct icao)`):
- купить AT-3 или AT-6 и использовать вместо AT-10
- либо оставить AT-10 + поднять gain выше auto

### Источники
- https://aerodata.ai/why-your-ads-b-receiver-gets-no-planes-at-night/
- https://github.com/Aerobits/dump1090-fa/blob/master/README.adaptive-gain.md
- https://github.com/wiedehopf/readsb
- https://discussions.flightaware.com/t/losing-adsb-signal-on-some-nearby-aircraft/87461
