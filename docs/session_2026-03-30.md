# Сессия 2026-03-30

## Что сделали

### 1. Почистили репо
- Добавили в `.gitignore`: `*.gz`, `*.bak`, `*.docx`, `frontend/download.html`
- `download.html` — HTML с base64-встроенным docx (КТА), не относится к проекту
- Коммит: `chore: ignore .gz, .bak, .docx and download.html`

### 2. Прочитали весь код
Полное изучение всех файлов: `ingest/main.py`, `ingest/db.py`, `ingest/sbs_parser.py`,
`server/api/main.py`, `feeder/feeder.py`, `feeder/feeder_json.py`, `server/db/init.sql`.

### 3. Обновили ARCHITECTURE.md
Старый ARCHITECTURE.md был устаревшим. Переписали полностью:
- Актуальная схема компонентов
- Протокол AUTH / AUTH-JSON
- Описание алгоритмов: merge по ICAO, ghost filter, дедупликация, lastPosition fallback
- Схема БД со всеми полями
- Открытые задачи
- Коммит: `docs: обновить ARCHITECTURE.md — актуальный код, алгоритмы, открытые задачи`

## Открытые задачи на следующие сессии
- [ ] `drop_old_partitions` — нет автозапуска, нужен cron
- [ ] Переход на `feeder_json.py` — богаче данные (rssi, signal_type, category)
- [ ] Шаг 3 — подтвердить движение самолётов на реальном борте с координатами

## Договорились
- В конце каждой сессии: обновлять ARCHITECTURE.md (если были изменения) + сохранять session_ДАТА.md
