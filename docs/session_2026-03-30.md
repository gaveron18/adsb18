# Сессия 2026-03-30

## Что сделали

### 1. Почистили репо
- Добавили в `.gitignore`: `*.gz`, `*.bak`, `*.docx`, `frontend/download.html`
- `download.html` — HTML с base64-встроенным docx (КТА), не относится к проекту
- Коммит: `chore: ignore .gz, .bak, .docx and download.html`

### 2. Прочитали весь код
Полное изучение всех файлов:
- `server/ingest/main.py` — TCP-сервер, AUTH / AUTH-JSON протокол
- `server/ingest/db.py` — merge по ICAO, ghost filter, батчи, дедупликация
- `server/ingest/sbs_parser.py` — парсер SBS строк
- `server/api/main.py` — FastAPI: aircraft.json, history, monitor, WebSocket
- `feeder/feeder.py` — SBS фидер с disk buffer и replay
- `feeder/feeder_json.py` — JSON фидер (не используется на Pi)
- `server/db/init.sql` — схема БД, партиции, функции
- Фронтенд: `script.js` (9140 строк), `planeObject.js`, `layers.js`, `archive.html`

### 3. Обновили ARCHITECTURE.md
Переписали полностью. Добавили разделы:
- Схема компонентов
- Протокол фидера
- Схема БД
- Ключевые алгоритмы (merge, ghost filter, дедупликация, lastPosition fallback)
- Зависимости и риски (10 рисков)
- Зависимости внутри кода (вызовы функций, общие переменные)
- Фронтенд — логика и поведение (главный цикл, PlaneObject, карта, архив, риски 11-13)

### 4. Выстроили систему удержания контекста
- В `CLAUDE.md` добавили правила:
  - При первом упоминании adsb18 → читать ARCHITECTURE.md + TROUBLESHOOTING.md + последний session_*.md
  - После каждого значимого шага → обновлять session_ДАТА.md
  - При "завершаем" → финал + ARCHITECTURE.md + git push
- Создали `memory/feedback_adsb18_code_discipline.md` с правилами работы с кодом
- Составили общую инструкцию для других проектов → `/home/hive/context_system_instruction.md`

## Открытые задачи на следующую сессию
- [ ] `drop_old_partitions` — нет автозапуска, нужен cron
- [ ] Переход на `feeder_json.py` — богаче данные (rssi, signal_type, category)
- [ ] Шаг 3 — подтвердить движение самолётов на реальном борте с координатами

## Договорились
- При первом упоминании adsb18 в сессии — Claude читает 3 файла с VPS автоматически
- После каждого шага — обновлять session_ДАТА.md (не ждать конца сессии)
- При "завершаем" — финал + ARCHITECTURE.md + git push
- ARCHITECTURE.md обновляется после значимых изменений в коде
