# Сессия 2026-04-07

## Фича: активная сессия в логе приёмника (коммит 61605d8)

### Проблема
Окно «Лог работы приёмника» не показывало текущую активную сессию Pi.
При открытии модала период брался из архивных дат (которые могли быть за прошлые дни),
и текущая сессия не попадала в запрос.

### Что сделано

**API (`server/api/main.py` — `/api/receiver-log`):**
- Добавлена проверка `pi_active`: если `max(ts)` в positions < 5 мин назад — Pi активен
- Последняя сессия в выборке получает `is_active: true` и `end_utc = now`
- Условие активности: `pi_active AND последняя сессия AND (now - end) < 10 min`
  — третье условие нужно чтобы старые сессии не помечались активными при фильтре по прошлым датам

**Frontend (`frontend/archive.html`):**
- CSS: `.rl-active td` — зелёный фон (#e8f8ee), зелёный текст (#1a7a3a)
- CSS: `.rl-active-badge` — значок «● работает»
- `openReceiverLog`: `rlTo` всегда расширяется до сегодня — активная сессия видна
  даже если архив открыт на прошлый день
- `renderRlTable`: активная строка — зелёная, время окончания «сейчас ►»

### Баг найден и исправлен в процессе тестирования
При фильтре по старым датам (например апрель 1-3) последняя сессия в выборке
помечалась активной — так как `pi_active=True` и `i == len(sessions)-1`.
Исправлено: добавлена проверка `(now - end) < timedelta(minutes=10)`.

### Тесты пройдены
- Старые даты → active: 0 ✓
- Сегодня → active: 1 ✓  
- Запрос без фильтра (30 дней) → active: 1 ✓
- Длительность активной сессии считается корректно ✓

---

## Фикс: receiver-log — Pi активен без самолётов (коммит 65f1d3e)

### Проблема
После предыдущей сессии выяснилось: если Pi работает, но самолётов в зоне нет,
окно лога не показывало активную сессию. pi_active = False, хотя Pi онлайн.

### Причина
pi_active проверял max(ts) FROM positions < 5 мин.
Без самолётов позиции не пишутся — last_pos_ts устарел (был 43 мин назад).

### Что сделано
- Добавлена _check_pi_alive() — curl к :30092, проверяет поле now < 30с
- pi_active = await _check_pi_alive() вместо проверки БД
- Убрана проверка (now - end) < 10 мин — не нужна при туннельной проверке

### Тесты
- Pi онлайн, самолётов нет — is_active: True
- API возвращает активную сессию сегодняшнего дня

---

## Кнопка 📍 координаты приёмника (коммит в сессии)

### Что сделано

**API (`server/api/main.py`):**
- GET `/api/pi-location` — SSH на Pi, читает текущие lat/lon из /etc/default/readsb
- POST `/api/pi-location` — выполняет `sudo readsb-set-location lat lon` через SSH,
  верифицирует результат обратным чтением с Pi, обновляет feeders таблицу

**Frontend (`frontend/index.html`):**
- Кнопка 📍 рядом со шестерёнкой в хедере
- Модальное окно с паролем (123, один раз за сессию) + форма координат
- Текущие координаты загружаются с Pi при открытии
- После успешной установки страница перезагружается автоматически через 1.5с
- Фикс: флаг `_lmDownOnOverlay` — закрытие только если mousedown был на overlay

**БД:** `feeders.lat/lon` изменён с REAL → DOUBLE PRECISION (потеря точности 56.836845→56.83759)

---

## Защита паролем

**archive.html:** оверлей с паролем 0000, один раз за сессию браузера.
Инлайн-скрипт в `<head>` добавляет класс `ap-locked` до загрузки страницы —
нет мелькания контента.

**index.html:** модал координат защищён паролем 123, флаг `_lmUnlocked` in-memory.

---

## Деплой на prod сервер 185.221.160.175

### Что сделано на prod VPS
- Репо клонировано в `/opt/adsb18/`
- venv в `/opt/adsb18-venv/`, зависимости установлены
- БД adsb18 создана, схема применена, GRANT прав выданы
- systemd сервисы: adsb18-api, adsb18-poller (PI_AIRCRAFT_URL=:30093)
- nginx настроен на порт 8098, nginx.conf из репо с заменой путей
- Карта открывается: http://185.221.160.175:8098

### SSH-туннель Pi→prod
- На Pi создан сервис `adsb-tunnel-prod.service` (autossh)
- Туннель: Pi:80 → prod:30093
- Pi передаёт данные на оба VPS одновременно

### Исправлено в процессе деплоя
- feeders.lat/lon тип REAL → DOUBLE PRECISION (потеря точности)
- GRANT прав на таблицы (init.sql создавался от postgres, adsb не имел доступа)
- Координаты Pi случайно перезаписаны нулями → восстановлены (56.836845, 53.249700)

---

## Обновление deploy.sh

`deploy.sh` приведён в соответствие с poller-архитектурой:
- `APP_DIR=/opt/adsb18` (фиксированный, не зависит от пользователя)
- `User=root` в systemd сервисах
- Убран `adsb18-ingest`, добавлен `adsb18-poller` с `PI_AIRCRAFT_URL`
- nginx — `sed` заменяет пути в `nginx.conf` из репо (не inline-конфиг)
- `PI_TUNNEL_PORT=30093` — порт туннеля для prod
- UFW: убран порт 30001 (ingest не нужен)
- Раздел "ПОСЛЕ ДЕПЛОЯ" обновлён под туннельную архитектуру
