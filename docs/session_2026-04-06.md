
## Баг: галочка CBJ666 слетала через 10-15 сек (и другие треки не отображались)

### Симптомы
- Ставишь галочку на рейс — маршрут не появляется, галочка слетает ~10с
- Кнопка «Все» показывает только 1 трек из 5

### Причина
Два бага в :

1. **** вызывал  без проверки на null.
   Если  оказывался null/undefined — Leaflet бросал .
   Этот TypeError ловился в 's catch → галочка слетала (трек уже был на карте, но checkbox снимался).

2. ****:  была внутри общего .
   Любая ошибка в recalcDistances снимала галочку, хотя трек уже добавлен в .

### Фикс (коммит eaf464f)
- : добавлен  перед removeLayer
- :  обёрнута в собственный 
  — ошибки расстояний не влияют на состояние галочки трека

## Баг: галочка рейса слетала через ~10 сек (и Все показывал только 1 трек)

### Симптомы
- Ставишь галочку на рейс CBJ666 — маршрут не появляется, галочка слетает через ~10с
- Кнопка Все показывает только 1 трек из 5

### Причина
Два бага в frontend/archive.html:

1. clearDistanceLayers вызывал map.removeLayer(labelMarker) без проверки на null.
   Leaflet бросал TypeError — этот error ловился в addTrack catch и снимал галочку,
   хотя трек уже был добавлен на карту.

2. recalcDistances() была внутри общего try-catch в addTrack.
   Любая ошибка в recalcDistances снимала галочку, хотя трек уже в activeTracks.

### Фикс (коммит eaf464f)
- clearDistanceLayers: добавлен null-check перед map.removeLayer(labelMarker)
- addTrack: recalcDistances() обёрнута в свой try-catch
  — ошибки расстояний не влияют на состояние галочки трека

## Дополнительные баги (второй раунд)

### Баг 3 — selectVisible race condition с clearAll
Если нажать Снять пока Все ещё грузит — clearAll снимал галочки, но selectVisible
продолжал добавлять треки с непроверенными чекбоксами.
Фикс: добавлена проверка if (!cb2.checked) continue после await (как в addTrack).

### Баг 4 — recalcDistances в removeTrack без try-catch
Ошибка в recalcDistances при снятии трека = необработанное исключение.
Фикс: обёрнуто в try-catch.

### Баг 5 — recalcDistances в конце selectVisible без try-catch
Аналогично — ошибка при нажатии Все = unhandled error.
Фикс: обёрнуто в try-catch.

### Баг 6 — onDotIntervalChange мог потерять треки
Порядок был: removeLayer → buildTrackLayer. Если buildTrackLayer бросал исключение,
трек исчезал с карты навсегда.
Фикс: сначала buildTrackLayer (новый слой), потом removeLayer (старый). 
Если buildTrackLayer бросает — старый слой остаётся на карте.

## Рефакторинг archive.html — 4 слоя (коммит 4fa5780)

Разбили addTrack/selectVisible на 4 слоя с единственной ответственностью:
- Layer 1 Network: fetchTrackPoints, fetchWithTimeout — только HTTP
- Layer 2 State: registerTrack, unregisterTrack — только activeTracks Map
- Layer 3 UI: setItemActive, isItemChecked — только checkbox/CSS класс
- Layer 4 Orchestrators: addTrack, removeTrack, selectVisible — вызывают слои 1-3

Найден и исправлен баг рефакторинга (коммит ffbc880):
removeTrack вызывал updateTrackCount (внутри unregisterTrack) до снятия галочки.
Фикс: setItemActive(el, false) перед unregisterTrack — updateTrackCount читает актуальный DOM.

## Healthcheck с Telegram алертами (коммит 5327395)

Создан /home/new/adsb18/healthcheck.py:
- Проверяет: API :9001, свежесть позиций в БД (порог 10 мин), фидер (порог 15 мин), туннель Pi :30092
- Алерты в Telegram бот @adsb18_monitor_bot (chat_id=357650937)
- venv: /opt/adsb18-venv, psycopg2-binary установлен через sudo pip
- Cron */5 * * * * — сейчас закомментирован (Pi выключен)
- Включить: crontab -l | sed 's|^#\*/5|\*/5|' | crontab -

При тестировании обнаружено: данные стоят с 05.04 19:36, Pi выключен — ожидаемо.

## Документация

- TESTING.md — 10 сценариев ручного тестирования archive.html (коммит 683d938)
- CLAUDE.md — полностью переписан: актуальная poller-архитектура, 19 правил минимизации багов, все задачи (коммит 6a3ed81)
- Alias на VPS: `adsb18` → cd /home/new/adsb18 (добавлен в ~/.bashrc пользователя new)

## Лог приёмника — новая кнопка 📋 (коммит 03faafd)

Добавлена кнопка 📋 рядом с ⚙. Открывает модальное окно с сессиями работы приёмника.
- Определение сессий: разрыв >15 мин в positions = новая сессия
- Показывает: код приёмника, дату, начало/конец UTC, количество маршрутов
- Фильтрация по датам (берёт период с архивной страницы)
- Время отображается в выбранном TZ
- Светлая тема модального окна

### Баги исправленные в процессе
- flights == routes (Mode-S без координат не считается) → убрана колонка Рейсов
- thStart/thEnd DOM обновлялись N раз в цикле → вынесено за цикл
- rlData не сбрасывался при ошибке fetch → добавлен rlData = [] в catch
- CSV экспортировал UTC вместо выбранного TZ → применён tzOffsetMs

## Светлая тема модалок (коммиты 24b237c, style)

- Модальное окно лога приёмника → светлая тема
- Модальное окно точек измерений → светлая тема (.modal-light класс)
- Добавлен .modal-light .btn-outline:hover фикс (тёмный hover на светлом фоне)

## Восстановление сессии через localStorage (коммиты eff18ac, 3a2ba9a, 4e7b954, f511c25, a997f97, f346c5a)

Состояние страницы сохраняется в localStorage и восстанавливается при открытии:
- Период (даты from/to) — adsb18_period
- Активные треки — adsb18_active_tracks
- Позиция и зум карты — adsb18_map_view
- Интервал точек (слайдер) — adsb18_dot_interval
- Включён/выключен расчёт расстояний — adsb18_distances

Каждый компьютер/браузер хранит свою сессию независимо.
Общая база точек измерений — одна для всех (PostgreSQL).

### Серия TDZ багов (Temporal Dead Zone)
Корневая причина: переменные объявленные через let/const ниже init-блока
вызывали ReferenceError при синхронном вызове clearAll→recalcDistances на старте.

Исправлено: перенесены в State section (строки 420-553) до init-блока:
- distancesEnabled, distanceLayers, distanceData
- pointMarkers, pointsData, editingPointId, flagIcon, rlData

### Ещё баги исправленные после TDZ

- savePeriod() вызывался до fetch — при 0 результатах сохранял плохой период → перенесён после успешной загрузки данных
- Дефолтный период сегодня 00:00 → сейчас не охватывал данные Pi → заменён на последние 24 часа
- Добавлена кнопка 🔄 Сбросить сессию — удаляет все adsb18_* из localStorage и делает reload

## Исправление 5 багов 2026-04-06 (текущий коммит)

### Баг 1 — onTZChange сбрасывал период
Строка 598: initDatePickers() без параметров сбрасывала поля на последние 24ч при смене TZ.
Исправление: убран вызов initDatePickers() из onTZChange — refreshDisplayedTimes() достаточно.

### Баг 2 — deletePoint без try-catch
await fetch в deletePoint не был обёрнут — сетевая ошибка давала unhandled rejection.
Исправление: добавлен try-catch с alert.

### Баг 3 — deleteFlight не удалял из allFlights
После удаления рейса из DOM он оставался в массиве allFlights.
Исправление: добавлена строка allFlights = allFlights.filter(f => f.icao !== icao).

### Баг 4 — clearAll вызывал recalcDistances без try-catch
Падение recalcDistances могло прерывать очистку треков.
Исправление: try { recalcDistances(); } catch {...}

### Баг 5 — resetSession не очищал adsb18_points_visible
localStorage ключ adsb18_points_visible не был в списке сброса.
Исправление: добавлен в массив в resetSession().

## Обсуждение: несколько Pi приёмников

Архитектура уже поддерживает несколько Pi:
- positions.feeder_id — каждая позиция привязана к фидеру ✅
- feeders таблица — может хранить N приёмников ✅
- poller.py принимает feeder_id как параметр ✅

Не готово: poller.py хардкодит один URL (порт 30092), нужен отдельный экземпляр на каждый Pi.
Актуально когда появится второй Pi — тогда доработаем.

## Запланировано на следующую сессию

- Смена координат Pi из веб-приложения (Pi сейчас выключен)
  - Команда на Pi: sudo readsb-set-location {lat} {lon}
  - VPS → SSH туннель порт 52222 → Pi
  - Нужно: проверить sudo, добавить POST /api/pi/location, форму в frontend

## Баг: треки с 200+ точками не грузились (AbortError) — 2026-04-06

### Симптомы
- Рейс UTA6126 (151D54) не показывал трек в archive.html
- В консоли браузера: 
- Другие рейсы того же дня загружались нормально

### Причина
В nginx конфиге  для location  стояло .
Рейс с 222 точками отдавал **47 939 байт** без сжатия.
Браузер не успевал скачать за 10-секундный таймаут  в archive.html.
Проявлялось непостоянно — зависело от скорости соединения в момент запроса.

Диагностика: nginx access log показал что запрос 151D54 приходил на сервер через 8 секунд
после старта (другие рейсы — мгновенно), иногда таймаут 10с срабатывал раньше.

### Исправление
В , location :

Результат: 47 939 байт → 5 853 байта (сжатие в 8 раз).


## Баг: треки с 200+ точками не грузились (AbortError) — 2026-04-06

### Симптомы
- Рейс UTA6126 (151D54) не показывал трек в archive.html
- В консоли браузера: addTrack error: AbortError: signal is aborted without reason
- Другие рейсы того же дня загружались нормально

### Причина
В nginx конфиге /etc/nginx/sites-enabled/adsb18 для location /api/ стояло gzip off.
Рейс с 222 точками отдавал 47 939 байт без сжатия.
Браузер не успевал скачать за 10-секундный таймаут fetchWithTimeout в archive.html.
Проявлялось непостоянно — зависело от скорости соединения в момент запроса.

Диагностика: nginx access log показал что запрос 151D54 приходил на сервер через 8 секунд
после старта (другие рейсы мгновенно), иногда таймаут 10с срабатывал раньше.

### Исправление
В /etc/nginx/sites-enabled/adsb18, location ^~ /api/:
  gzip on;
  gzip_types application/json;
  gzip_min_length 1024;

Результат: 47 939 байт → 5 853 байта (сжатие в 8 раз).
sudo nginx -t && sudo systemctl reload nginx
